export enum MillisecondsTime {
  Forever = -1,
  Millisecond = 1,
  Second = 1000 * Millisecond,
  Miniute = 60 * Second,
  Hour = 60 * Miniute,
  Day = 24 * Hour,
}

export enum SecondsTime {
  Forever = -1,
  Second = 1,
  Miniute = 60 * Second,
  Hour = 60 * Miniute,
  Day = 24 * Hour,
}
